# 데이터베이스 성능과 MySQL 아키텍처

## 성능의 중요성

> **성능** = 쿼리가 결과를 돌려주기까지 걸리는 **시간**

- 데이터가 적으면 어떤 쿼리든 빠르다.
- 데이터가 수백만 건이 되면 같은 쿼리도 수 초 이상 걸린다.

**중요한 점:** 데이터를 **찾는 것**보다 **빠르게 찾는 것**이 더 어렵다.

---

## MySQL 서버 구조

> 쿼리는 **연결 → 파서 → 옵티마이저 → 실행 → 스토리지 엔진** 순서로 처리된다.

### 1. 연결

> 클라이언트(앱, 서버)의 **접속을 받는** 단계

### 2. 파서

> SQL **문법이 맞는지 검사**하는 단계

문법이 틀리면 여기서 바로 에러가 난다.

```sql
SELET * FROM product;   -- SELECT 오타
-- ERROR 1064: SQL 문법 오류
```

### 3. 옵티마이저

> **가장 빠른 실행 방법**을 정하는 단계 (인덱스를 쓸지, 전체를 읽을지 등)

### 4. 실행

> 옵티마이저가 정한 방법대로 **쿼리를 실행**하는 단계

### 5. 스토리지 엔진

> 디스크에서 **실제 데이터를 읽고 쓰는** 단계. 테이블마다 엔진을 **바꿀 수 있다.**

```sql
SHOW ENGINES;   -- 사용 가능한 스토리지 엔진 목록
```

**중요한 점:** 1~4단계는 **"무엇을 어떻게 할지"**, 5단계는 **"실제로 데이터를 꺼내는 일"**을 한다.

---

## InnoDB

> **InnoDB** = MySQL의 **기본 스토리지 엔진**

### 특징

- **트랜잭션**: 여러 작업을 전부 성공 또는 전부 취소로 묶는다.
- **행 단위 잠금**: 수정 중인 행만 잠가서 동시 작업이 가능하다.
- **장애 복구**: 서버가 꺼져도 로그로 데이터를 되살린다.

### 구조

| 영역 | 구성 요소 | 하는 일 |
|---|---|---|
| 메모리 | **버퍼 풀** | 자주 쓰는 데이터를 올려둔다 |
| 디스크 | 데이터 파일 | 실제 데이터가 저장된다 |
| 디스크 | 로그 파일 | 변경 내용을 기록해 복구에 쓴다 |

---

## 버퍼 풀

> **버퍼 풀** = InnoDB가 데이터를 **메모리**에 올려두는 공간

- 메모리는 빠르고, 디스크는 느리다.
- 한 번 읽은 데이터는 버퍼 풀에 남겨두고 다음부터 여기서 읽는다.

```sql
SELECT @@innodb_buffer_pool_size / 1024 / 1024 AS buffer_pool_mb;
-- 결과: 128  → 128MB (기본값)
```

**중요한 점:** 버퍼 풀이 클수록 디스크 읽기가 줄어 빨라진다.

---

## 페이지

> **페이지** = InnoDB가 데이터를 읽고 쓰는 **최소 단위**. 크기는 **16KB**

- 1줄만 필요해도 그 줄이 든 **페이지 전체**를 읽는다.
- 테이블이 클수록 페이지 수가 늘고, 읽을 양도 늘어난다.

```sql
SELECT @@innodb_page_size / 1024 AS page_size_kb;
-- 결과: 16
```

**중요한 점:** MySQL은 **줄 단위가 아니라 페이지 단위**로 읽는다.

---

## 쿼리 실행 흐름

> **EXPLAIN** = 쿼리를 실행하지 않고 **실행 계획**만 보여주는 명령어

```sql
EXPLAIN SELECT * FROM member WHERE member_id = 1;
```

| 항목 | 값 | 뜻 |
|---|---|---|
| type | const | 결과가 딱 1건, 가장 빠른 방식 |
| key | PRIMARY | 기본키 인덱스 사용 |
| rows | 1 | 1줄만 확인 |

**중요한 점:** 기본키로 조회하면 가장 빠르다.

---

## 버퍼 풀 히트율

> **히트율** = 필요한 데이터가 **버퍼 풀에 이미 있었던 비율**

```sql
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_read%';
```

| 변수 | 뜻 |
|---|---|
| read_requests | 데이터 요청 전체 횟수 |
| reads | 버퍼 풀에 없어서 **디스크까지 간** 횟수 |

> **히트율 = (1 − reads ÷ read_requests) × 100**

**중요한 점:** 히트율 **99% 이상**이 정상이다.

---

## 병목

> **병목** = 전체 속도를 떨어뜨리는 **가장 느린 지점**. 대부분 **디스크를 많이 읽을 때** 생긴다.

```sql
SELECT * FROM product WHERE product_name LIKE '%10000%';   -- 느림
SELECT * FROM product WHERE product_id = 2500000;          -- 빠름
```

| 쿼리 | 인덱스 | 읽는 행 수 |
|---|---|---|
| `LIKE '%10000%'` | 사용 X | 전체 (약 500만 건) |
| `product_id = 2500000` | 사용 O | 1건 |

> `%`가 **앞에** 붙으면 시작 글자를 알 수 없어 **인덱스를 못 쓴다.**

**중요한 점:** 빠른 쿼리의 핵심은 **덜 읽게 만드는 것**이다.