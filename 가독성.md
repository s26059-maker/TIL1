#  가독성 높은 코드를 작성하는 법

## 생산성 개선

코드는 쓰는 시간보다 **읽는 시간**이 압도적으로 길다. "빨리 쓴 코드"보다 "빨리 읽히는 코드"가 팀 전체의 속도를 올린다.

### 검증의 자동화

사람이 눈으로 확인하는 걸 기계에게 넘기는 것. 포맷·네이밍·잠재적 버그처럼 **정답이 정해진 것**은 도구가 잡게 하고, 사람은 설계와 의도만 본다.

- 포매터 / 린터 (ktlint, detekt)
- 컴파일러 경고
- 테스트 코드, CI

```kotlin
// 도구가 잡아주는 것 — 리뷰에서 굳이 말할 필요 없음
val x = listOf(1,2,3)   // 공백 누락 → ktlint

// 사람이 봐야 하는 것 — 도구는 절대 못 잡음
fun calculate(u: User): Int = u.point * 3 - 10   // 왜 3배? 왜 -10?
```

### 논의 빈도

리뷰에서 같은 지적이 반복된다면 그건 개인의 실수가 아니라 **팀에 규칙이 없다는 신호**다.

- 매번 나오는 지적 → 컨벤션 문서 또는 린트 룰로 승격
- 한 번만 나오는 지적 → 그냥 코멘트로 해결

논의는 줄이는 게 목적이 아니라, **같은 논의를 두 번 하지 않는 것**이 목적이다.


## 가독성의 요건

### 단순한 코드

읽는 사람이 머릿속에 담아야 할 정보가 적은 코드. 한 줄에 여러 일을 욱여넣지 않는다.

```kotlin
// 나쁨 — 한 줄에 필터 + 변환 + 조건이 뒤엉킴
fun f(l: List<User>) = l.filter { it.age >= 19 && it.status == "A" && !it.banned }
    .map { it.name.trim().lowercase() }.filter { it.isNotEmpty() }

// 좋음 — 조건에 이름을 붙여 한 단계씩
fun activeAdultNames(users: List<User>): List<String> =
    users.filter { it.isActiveAdult() }
         .map { it.name.normalized() }
         .filter { it.isNotEmpty() }

private fun User.isActiveAdult() = age >= 19 && status == "A" && !banned
private fun String.normalized() = trim().lowercase()
```

깊은 중첩도 단순함의 적이다. **빠른 반환(early return)** 으로 펴준다.

```kotlin
// 나쁨 — 계단식 중첩
fun save(user: User?) {
    if (user != null) {
        if (user.isValid) {
            if (!user.isBanned) {
                repository.save(user)
            }
        }
    }
}

// 좋음 — 예외 상황을 위에서 걷어냄
fun save(user: User?) {
    if (user == null) return
    if (!user.isValid) return
    if (user.isBanned) return
    repository.save(user)
}
```

### 의도가 명확한 코드

코드는 **"무엇을 하는지"** 가 아니라 **"왜 하는지"** 가 보여야 한다. 이름이 주석을 대신하게 만든다.

```kotlin
// 나쁨 — 주석이 없으면 해석 불가
fun check(u: User): Boolean {
    return u.p > 1000 && u.d < 30   // ???
}

// 좋음 — 이름이 곧 설명
fun isEligibleForVipUpgrade(user: User): Boolean {
    val hasEnoughPoints = user.point > VIP_POINT_THRESHOLD
    val isRecentlyActive = user.daysSinceLastLogin < ACTIVE_DAYS
    return hasEnoughPoints && isRecentlyActive
}
```

숫자와 문자열을 그대로 박아두는 것(**매직 넘버**)은 의도를 지운다. 상수로 이름을 붙인다.

```kotlin
// 나쁨
if (order.amount > 50000) applyFreeShipping()

// 좋음
private const val FREE_SHIPPING_THRESHOLD = 50_000
if (order.amount > FREE_SHIPPING_THRESHOLD) applyFreeShipping()
```

주석은 **코드로 표현 못 하는 배경**에만 쓴다.

```kotlin
// 나쁨 — 코드를 그대로 읽은 주석
// user의 point를 3배 한다
val bonus = user.point * 3

// 좋음 — 코드가 말 못 하는 이유
// 2024 신규 가입 이벤트 정책(기획 문서 #124)에 따라 3배 적립
val bonus = user.point * EVENT_BONUS_RATE
```

### 독립성이 높은 코드

하나를 고칠 때 다른 곳이 딸려 오지 않는 상태. 부품이 나사로 끼워져 있으면 갈아 끼우기 쉽고, 용접돼 있으면 통째로 잘라내야 한다. 코드도 똑같다.

여기서 자주 쓰는 두 단어가 있다.

- **결합도(coupling)**: 다른 코드에 얼마나 의존하는가 → **낮을수록 좋다**
- **응집도(cohesion)**: 한 곳에 모인 코드들이 얼마나 같은 목적인가 → **높을수록 좋다**

```kotlin
// 나쁨 — 구체 클래스에 직접 묶임. 결제사 바꾸면 서비스도 뜯어야 함
class OrderService {
    private val payment = TossPaymentClient()   // 용접됨
    fun order(o: Order) = payment.pay(o.amount)
}

// 좋음 — 인터페이스에만 의존. 구현체는 갈아 끼우면 끝
class OrderService(private val payment: PaymentClient) {   // 나사로 끼움
    fun order(o: Order) = payment.pay(o.amount)
}
```

전역 상태(어디서든 바꿀 수 있는 값)는 독립성을 무너뜨린다. 값은 **필요한 곳에 넘겨주는 것**이 기본이다.

```kotlin
// 나쁨 — 누가 언제 바꿨는지 추적 불가
object Session { var currentUserId: Long? = null }

// 좋음 — 흐름이 눈에 보임
fun createPost(userId: Long, content: String) { ... }
```

### 구조화된 코드

비슷한 것끼리 모으고, 다른 것은 떼어놓는 것. 파일을 열었을 때 "여기 뭐가 있겠구나"가 예측되면 성공이다.

```kotlin
// 나쁨 — 한 함수가 검증 + 계산 + 저장 + 알림을 다 함
fun processOrder(request: OrderRequest) {
    if (request.items.isEmpty()) throw IllegalArgumentException()
    if (request.address.isBlank()) throw IllegalArgumentException()
    var total = 0
    for (item in request.items) total += item.price * item.count
    if (total > 50000) total -= 3000
    orderRepository.save(Order(request.userId, total))
    mailSender.send(request.email, "주문 완료")
}

// 좋음 — 단계마다 이름이 붙어 흐름이 목차처럼 읽힘
fun processOrder(request: OrderRequest) {
    validate(request)
    val total = calculateTotal(request.items)
    val order = orderRepository.save(request.toOrder(total))
    notifyOrderCompleted(order)
}
```

패키지도 같은 원리다. 기술 단위보다 **도메인 단위**로 묶으면 관련된 코드가 한 폴더에 모인다.

```text
// 기술 중심 — 주문 하나 고치려고 폴더 4개를 돌아다님
controller/  service/  repository/  dto/

// 도메인 중심 — 주문 관련은 order 폴더 안에 다 있음
order/    OrderController.kt  OrderService.kt  OrderRepository.kt
payment/  PaymentController.kt ...
```

## 대표적인 프로그래밍 원칙

### 보이스카우트 원칙

"캠핑장을 떠날 때는 왔을 때보다 깨끗하게." 코드를 만졌으면 **아주 조금이라도 나아진 상태로** 커밋한다.

- 오타난 변수명 하나 고치기
- 죽은 코드(아무도 안 쓰는 함수) 지우기
- 이해 안 되던 이름에 의미 부여하기

```kotlin
// 버그 고치러 들어왔다가, 이름 하나만 손보고 나가기
- fun getD(u: User) = u.createdAt.until(now())
+ fun daysSinceJoined(user: User) = user.createdAt.until(now())
```
### YAGNI

You Aren't Gonna Need It — "그거 결국 안 쓴다."
"나중에 필요할 것 같아서" 만든 코드는 대부분 안 쓰이고, 그동안 읽는 사람을 헷갈리게만 한다.

```kotlin
// 나쁨 — 지금은 CARD만 쓰는데 미리 다 만들어 둠
interface PaymentClient {
    fun pay(amount: Int)
    fun payWithCrypto(amount: Int)     // 계획 없음
    fun payInInstallments(month: Int)  // 기획에 없음
}

// 좋음 — 지금 필요한 것만. 필요해지면 그때 추가
interface PaymentClient {
    fun pay(amount: Int)
}
```

### KISS

Keep It Simple, Stupid — 단순하게 유지하라.
"멋진 코드"가 아니라 **"설명이 필요 없는 코드"** 가 이긴다.

```kotlin
// 나쁨 — 짧지만 해석에 시간이 걸림
val r = l.groupBy { it.t }.mapValues { (_, v) -> v.sumOf { it.p } }
    .toList().sortedByDescending { it.second }.take(3).toMap()

// 좋음 — 길어도 한 번에 읽힘
val totalByType: Map<Type, Int> = orders
    .groupBy { it.type }
    .mapValues { (_, orders) -> orders.sumOf { it.price } }

val top3 = totalByType.entries
    .sortedByDescending { it.value }
    .take(3)
    .associate { it.key to it.value }
```

### 단일 책임 원칙

Single Responsibility Principle(SRP). 하나의 클래스/함수는 **바뀌는 이유가 하나**여야 한다.

"기능이 하나"가 아니라 **"고쳐야 할 이유가 하나"** 라는 게 핵심이다.

```kotlin
// 나쁨 — 바뀌는 이유가 3개(가격 정책, 메일 양식, DB 스키마)
class OrderService {
    fun calculatePrice(order: Order): Int { ... }
    fun sendConfirmationMail(order: Order) { ... }
    fun saveToDatabase(order: Order) { ... }
}

// 좋음 — 각자 자기 이유로만 바뀜
class PriceCalculator { fun calculate(order: Order): Int { ... } }
class OrderMailSender { fun sendConfirmation(order: Order) { ... } }
class OrderRepository { fun save(order: Order) { ... } }
```

### 성급한 최적화는 모든 악의 근원

도널드 커누스의 말. **측정하기 전에 하는 최적화**는 가독성만 깎아먹고 성능은 그대로인 경우가 대부분이다.

```kotlin
// 나쁨 — "루프가 빠르대서" 직접 돌림. 병목도 아닌데 읽기만 어려움
fun sumPrices(orders: List<Order>): Int {
    var sum = 0
    var i = 0
    val size = orders.size
    while (i < size) { sum += orders[i].price; i++ }
    return sum
}

// 좋음 — 의도가 그대로 드러남. 느리면 그때 프로파일링 후 바꾼다
fun sumPrices(orders: List<Order>): Int = orders.sumOf { it.price }
```

순서는 항상 이렇다.

1. 읽기 쉽게 짠다
2. 느린지 **측정**한다 (프로파일링, 쿼리 로그)
3. 진짜 병목만 최적화하고, **왜 이렇게 했는지 주석으로 남긴다**

```kotlin
// N+1 쿼리로 응답 3초 → fetch join으로 200ms 개선 (측정: 2024-05 부하 테스트)
@Query("select o from Order o join fetch o.items where o.userId = :userId")
fun findAllWithItems(userId: Long): List<Order>
```